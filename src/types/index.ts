export enum GAS_PRICE {
  default = "5",
  fast = "6",
  instant = "7",
  testnet = "10",
}

export interface Address {
  137: string;
  2442: string;
}

export enum ChainId {
  MAINNET = 137,
  TESTNET = 2442,
}
